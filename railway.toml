[build]
builder = "DOCKERFILE"
dockerfilePath = "prometeo-tattoo/backend/Dockerfile"
buildContext = "prometeo-tattoo/backend"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
